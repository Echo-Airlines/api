# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres 
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the 
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.
LOG_LEVELS=log,error,warn,debug,verbose,fatal
DATABASE_URL=postgresql://dba:changeme@postgres:5432/changeme
SEED_DATABASE=true
FRONTEND_URL=https://www.echoairlines.com
API_URL=https://www.echoairlines.com/api
FILE_URL=https://www.echoairlines.com/files
NODE_ENV=development
API_HOST=localhost
API_PORT=3001
API_PREFIX=api
JOB_POLLING_INTERVAL_MS=60000
ONAIRSYNCENABLED=true
ONAIRVASYNCENABLED=true
ONAIRVAMEMBERSSYNCENABLED=false
ONAIRCOMPANYSYNCENABLED=false
ONAIRVIRTUALAIRLINEID=
ONAIRAPIKEY=
DISCORDSERVERINVITELINK=
DISCORDSERVERINVITELINKENABLED=false
ACCEPTINGNEWMEMBERS=false
DISCORDAUTHENABLED=false
LOCALAUTHENABLED=true

# Discord OAuth2 Configuration
# These are fallback values - the actual values should be stored in the database
DISCORD_CLIENT_ID=your_discord_client_id
DISCORD_CLIENT_SECRET=your_discord_client_secret
DISCORD_CALLBACK_URL=http://localhost:3001/api/auth/discord/callback